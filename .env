DATABASE_URL=file:./dev.db
AUTH_SECRET=supersecretkey123

# ===== STRIPE =====
# Reemplaza por tus claves reales del Dashboard de Stripe (modo test al principio).
# Publishable: dashboard.stripe.com/test/apikeys -> "Publishable key"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_TYooMQauvdEDq54NiTphI7jx
# Secret: dashboard.stripe.com/test/apikeys -> "Secret key" (NUNCA la subas a git)
STRIPE_SECRET_KEY=sk_test_REEMPLAZAR
# Webhook: dashboard.stripe.com/test/webhooks -> crea endpoint -> "Signing secret"
# Para desarrollo local usa `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
STRIPE_WEBHOOK_SECRET=whsec_REEMPLAZAR

# URL pública del sitio (sin barra final). Se usa para los return_url de Stripe.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ===== EMAIL (opcional, ya existente) =====
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# ADMIN_EMAIL=pedidos@sotodelprior.com
